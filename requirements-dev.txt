# ─────────────────────────────────────────────────────────────────────────
# requirements-dev.txt — 개발/테스트할 때만 추가로 필요한 패키지.
#
#   설치:  pip install -r requirements-dev.txt
#
# 첫 줄 `-r requirements.txt` 는 "운영용 목록을 먼저 그대로 포함" 하라는 뜻이다.
# 그래서 이 파일 하나만 설치해도 운영용 + 테스트용이 모두 깔린다.
# ─────────────────────────────────────────────────────────────────────────

-r requirements.txt

pytest>=8,<10               # 범위: 8 이상 10 미만

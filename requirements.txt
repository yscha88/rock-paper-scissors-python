# ─────────────────────────────────────────────────────────────────────────
# requirements.txt — "이 앱을 돌리려면 어떤 외부 패키지가 필요한가" 를 적은 목록.
#
#   설치:  pip install -r requirements.txt
#
# pyproject.toml 과 무엇이 다른지, 아래 버전 표기(==, >=, <=, ~= ...)가 각각
# 무슨 뜻인지는 docs/02-python-환경.md 의
#   · "pyproject.toml vs requirements.txt"
#   · "버전 고정(fix) vs 범위(over/down)"
# 절에서 자세히 설명한다.
# ─────────────────────────────────────────────────────────────────────────

# 가위바위보 "핵심 로직 + CLI" 는 표준 라이브러리만 쓰므로 필요한 외부 패키지가 없다.
# 아래 세 줄은 "웹 API(rps.web)" 를 돌릴 때만 필요하다.

fastapi>=0.110,<1.0         # 범위: 0.110 이상 1.0 미만 (하한 + 상한)
uvicorn[standard]>=0.29     # 하한만: 0.29 이상, 상한 없음
gunicorn>=21.2              # 하한만: 21.2 이상  (주의: gunicorn 은 Linux/macOS 전용, Windows 미지원)
